### ページテーブル
物理メモリと仮想メモリの対応表。CPUが仮想アドレスを物理アドレスに変換するために参照するデータ構造。

### MMU（Memory Management Unit）
CPUとメモリの間にあるハードウェア回路で、仮想アドレス→物理アドレス変換やアクセス制御（保護）を行う装置。

### TCB（Task Control Block）
RTOSやOSがタスク（スレッド/プロセス）ごとに保持する管理情報の構造体。タスクの「状態」を保存するレコード。

### FPU（Floating Point Unit）
浮動小数点演算（小数点を含む計算）を高速に行う専用ハードウェア。CPUコアに内臓されていることが多い。

### ベクトルユニット（SIMD/Vector Unit）
同じ演算を複数データに対して同時に行う（SIMD：Single Instruction Multiple Data）ためのハードウェア。画像処理や信号処理で高い効果を発揮する。

### 割り込みマスク（Interrupt Mask）
CPUやコアがどの割り込みを受け付けるかを制御するビットマスク。特定の割り込みを一時的に無効化（マスク）して、クリティカルな処理を保護する。

### MCU（Microcontroller Unit）
CPU・メモリ・周辺機能をまとめた組み込み向けのハードウェア。MCU上のCPUでRTOSが複数の処理を管理するソフトウェアとして動作し、アプリ→RTOS→MCU→HWという制御を行う。

### ミューテックス
複数のタスクが同じ資源を同時に使用しないようにするための仕組み

### 実行トレース
プログラムやRTOSが実際にどのような順序・タイミングで動いたのかを記録すること

### BSP（Board Support Package）
特定の基盤・MCU上でOSやアプリケーションを動かすために必要なハードウェア依存をまとめたもの
- CPUの初期化
- クロック設定
- 割り込み設定
- タイマー設定
- GPIO設定
- UART設定
- メモリ配置
- Startup Code

### IRQ（Interrupt Request）
割り込み要求。

### ISR（Interrupt Service Routine）
割り込みサービスルーチン、割り込み処理、割り込みハンドラなど。

### サービスコール

### TSS（Time Sharing System）
複数タスクを公平に実行するスケジューリング方式。代表例として、ラウンドロビン方式が挙げられる。